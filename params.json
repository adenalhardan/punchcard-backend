{
    "prefix": "punchcard:",
    "id_length": 5,

    "field_types": ["integer", "string"],
    "field_presences": ["required", "optional"],

    "form_keys": ["id", "host_id", "event_title", "fields"],
    "event_keys": ["host_id", "title", "host_name", "fields", "expiration"],

    "event_lifetime": 3600
}